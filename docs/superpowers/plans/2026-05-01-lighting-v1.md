# Lighting V1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add editor-only MCP tools for night atmosphere, moon/sun setup, fog, exposure, sky intensity, and bulk point/rect/spot lights.

**Architecture:** Extend the shared Rust protocol first, expose compact MCP tool methods and schemas next, then implement Unreal editor bridge commands using actor/component APIs. Keep the high-level night preset reusable and keep city light placement bulk-first.

**Tech Stack:** Rust, Tokio, serde, MCP stdio JSON-RPC, Unreal Engine 5.7 editor C++ plugin, Unreal light/sky/fog/post-process actor APIs.

---

## File Structure

- Modify `crates/unreal-mcp-protocol/src/commands.rs`: add lighting command and result types.
- Modify `crates/unreal-mcp-protocol/src/lib.rs`: export lighting structs.
- Modify `crates/unreal-mcp-protocol/tests/codec_roundtrip.rs`: cover lighting command/result round trips.
- Modify `crates/unreal-mcp-tests/src/lib.rs`: fake bridge responses for lighting commands.
- Modify `crates/unreal-mcp-server/src/tools.rs`: add tool methods, defaulting, and argument mapping.
- Modify `crates/unreal-mcp-server/src/mcp_stdio.rs`: add tool listing, schemas, and dispatch.
- Modify `crates/unreal-mcp-server/tests/mcp_stdio.rs`: cover schema listing and representative calls.
- Modify `crates/unreal-mcp-server/tests/connection_tools.rs`: update capability coverage if needed.
- Modify `unreal/UnrealMCPBridge/Source/UnrealMCPBridge/Private/Bridge/BridgeServer.cpp`: implement Unreal commands.
- Modify `docs/protocol/tools.md`: document new tool shapes.

## Tasks

### Task 1: Protocol

- [ ] Write failing round-trip tests for `lighting.set_night_scene`, `lighting.bulk_set_lights`, and compact lighting results.
- [ ] Add `LightSpec`, `LightingOperation`, `LightSummary`, `BulkLightOperation`, and lighting command variants.
- [ ] Export the new structs from `crates/unreal-mcp-protocol/src/lib.rs`.
- [ ] Run `cargo test -p unreal-mcp-protocol codec_roundtrip`.
- [ ] Commit with `feat(protocol): add lighting commands`.

### Task 2: Rust MCP Surface

- [ ] Write failing MCP stdio tests for tool listing and representative `lighting.set_night_scene` / `lighting.bulk_set_lights` calls.
- [ ] Update fake bridge with deterministic compact lighting responses.
- [ ] Add tool methods, argument structs, defaults, schemas, and dispatch.
- [ ] Run `cargo test -p unreal-mcp-server --test mcp_stdio` and `cargo test -p unreal-mcp-server --test connection_tools`.
- [ ] Commit with `feat(server): add lighting tools`.

### Task 3: Unreal Bridge

- [ ] Add Unreal handlers for `lighting.set_night_scene`, `lighting.set_sky`, `lighting.set_fog`, `lighting.set_post_process`, `lighting.bulk_set_lights`, and `lighting.set_time_of_day`.
- [ ] Reuse or spawn named support actors: `MCP_MoonLight`, `MCP_SkyLight`, `MCP_SkyAtmosphere`, `MCP_NightFog`, and `MCP_PostProcess`.
- [ ] Disable non-selected directional lights when applying a night scene or time-of-day preset.
- [ ] Spawn point, rect, and spot lights in bulk, apply transforms/components, and tag them with `mcp.generated` and `mcp.lighting`.
- [ ] Build standalone plugin with UE 5.7.
- [ ] Commit with `feat(unreal): add lighting bridge commands`.

### Task 4: Docs, Install, Live Smoke

- [ ] Document new tools in `docs/protocol/tools.md`.
- [ ] Copy plugin source into `MCPTester/Plugins/UnrealMCPBridge`.
- [ ] Rebuild `MCPTester` editor target.
- [ ] Run live stdio smoke calls for night scene setup, bulk lights, query, and smoke-light cleanup.
- [ ] Commit with `docs: document lighting tools`.

### Task 5: Final Verification

- [ ] Run `cargo fmt --check`.
- [ ] Run `cargo test`.
- [ ] Run `cargo clippy --workspace --all-targets -- -D warnings`.
- [ ] Run UE 5.7 standalone plugin build.
- [ ] Rebuild installed `MCPTester` plugin.
- [ ] Run live stdio smoke against `MCPTester`.
